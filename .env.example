# Database Configuration
# Path to SQLite database file
# Default: var/database.sqlite
DB_PATH=var/database.sqlite

# Server Configuration (for reference)
# Use built-in PHP server: php -S localhost:8000 -t public
# Or configure your web server to point to the public/ directory
